import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    maxHeight: '80vh',
    maxWidth: '30vw',
  },
  editProfile: {
    display: 'flex',
  },
}));

export default useStyles;
